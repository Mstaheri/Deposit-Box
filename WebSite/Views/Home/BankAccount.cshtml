
<!DOCTYPE html>
<html lang="fa">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #e0eafc, #cfdef3);
            font-family: 'Tahoma', sans-serif;
            margin: 0;
            padding: 0;
            direction: rtl;
            text-align: right;
        }

        .header-title {
            background: linear-gradient(to right, #007bb5, #004a8c);
            color: #fff;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            font-size: 1.75rem;
            font-weight: bold;
            margin-right: 310px;
            margin-top: 15px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
            transition: all 0.3s;
            animation: slideIn 0.5s ease-out forwards;
        }

        .header-title::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.15);
            border-radius: 8px;
            z-index: 0;
            transition: opacity 0.3s;
        }

        .header-title:hover::before {
            opacity: 0;
        }

        .header-title span {
            position: relative;
            z-index: 1;
        }

        .card {
            margin-bottom: 20px;
            border-radius: 15px;
            box-shadow: 0 20px 30px rgba(0, 0, 0, 0.2);
            background: linear-gradient(135deg, #ffffff, #f0f0f0);
            border: none;
            position: relative;
            overflow: hidden;
            transition: transform 0.3s;
            transform-style: preserve-3d;
            perspective: 1000px;
        }

        .card:hover {
            transform: rotateY(10deg) rotateX(10deg);
        }

        .card-header {
            background: linear-gradient(to right, #007bff, #0056b3);
            color: white;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            padding: 15px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card-header h5 {
            margin: 0;
            font-size: 1.25rem;
        }

        .card-body {
            padding: 20px;
            background: linear-gradient(135deg, #f8f8f8, #e0e0e0);
        }

        .card-text {
            font-size: 1rem;
            margin-bottom: 10px;
            color: #333;
        }

        .card-footer {
            background: #007bff;
            color: white;
            border-bottom-left-radius: 15px;
            border-bottom-right-radius: 15px;
            padding: 10px;
            display: flex;
            justify-content: center;
        }

        .card-footer button {
            background: #28a745;
            border: none;
            color: white;
            border-radius: 50px;
            padding: 10px 20px;
            margin: 0 5px;
            transition: background 0.3s;
            animation: pulse 1.5s infinite;
        }

        .card-footer button:hover {
            background: #218838;
        }

        .navbar {
            background-color: #3c8dbc;
            padding: 15px;
            direction: ltr;
            text-align: left;
        }

        .navbar-brand {
            color: #fff;
            font-weight: bold;
            font-size: 1.5rem;
        }

        .navbar-nav {
            direction: ltr;
            text-align: left;
        }

        .navbar-nav .nav-link {
            color: #fff;
            font-size: 1rem;
            padding: 10px 15px;
        }

        .navbar-nav .nav-link:hover {
            background-color: #357ca5;
            border-radius: 5px;
        }

        .sidebar {
            min-height: 100vh;
            background-color: #2c3e50;
            color: #ecf0f1;
            padding-top: 20px;
            position: fixed;
            top: 0;
            bottom: 0;
            right: 0;
            width: 250px;
            transition: width 0.3s;
        }

        .sidebar:hover {
            width: 300px;
        }

        .sidebar a {
            color: #ecf0f1;
            padding: 10px 20px;
            display: block;
            border-radius: 5px;
            transition: background-color 0.3s, padding-right 0.3s;
            text-decoration: none;
        }

        .sidebar a:hover {
            background-color: #34495e;
            padding-right: 30px;
        }

        .sidebar a.active {
            background-color: #3c8dbc;
        }

        .container {
            margin-top: 30px;
            margin-right: 160px;
            padding: 20px;
        }

        h2 {
            color: #3c8dbc;
            margin-bottom: 30px;
            text-align: center;
        }

        .btn-custom {
            background-color: #3c8dbc;
            color: white;
            border: none;
        }

        .btn-custom:hover {
            background-color: #357ca5;
        }

        .modal-body {
            direction: rtl;
        }

        .alert {
            margin-bottom: 20px;
        }

        .form-control {
            border-radius: 10px;
        }

        .modal-content {
            border-radius: 20px;
        }

        .modal-header {
            background: linear-gradient(to right, #007bff, #0056b3);
            color: white;
            border-top-left-radius: 20px;
            border-top-right-radius: 20px;
        }

        .modal-footer {
            border-bottom-left-radius: 20px;
            border-bottom-right-radius: 20px;
        }

        .close {
            color: white;
        }

       
    </style>
</head>

<body>
    
    <div class="sidebar">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link active" href="@Url.Action("Index", "Home")">داشبورد</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("BankSafe", "Home")">صندوق‌ها</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("BankSafeTransactions", "Home")">تراکنش ها</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("BankSafeDocument", "Home")">چک‌ها</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">امانات</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("User", "Home")">کاربران</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("BankAccount", "Home")">حساب کاربران</a>
            </li>
        </ul>
    </div>
    <div class="header-title">
        <span>حساب‌های بانکی</span>
    </div>
    <div class="container">
        
        <div id="alertContainer" style="display:none;" class="alert alert-success alert-dismissible fade show" role="alert">
            <span id="alertMessage"></span>
            <button type="button" class="close" data-dismiss="alert" aria-label="بستن">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <button class="btn btn-success mb-3" data-toggle="modal" data-target="#addAccountModal">اضافه کردن حساب بانکی جدید</button>
        <div id="accountsContainer" class="row">
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title">1234 5678 9012 3456</h5>
                        <div class="card-logo">
                            <img src="path/to/logo.png" alt="Bank Logo" style="height: 30px;">
                        </div>
                    </div>
                    <div class="card-body">
                        <p class="card-text"><strong>نام کاربر:</strong> محمد احمدی</p>
                        <p class="card-text"><strong>نام حساب:</strong> حساب جاری</p>
                        <p class="card-text"><strong>نام بانک:</strong> بانک ملت</p>
                        <p class="card-text"><strong>توضیحات:</strong> حساب ویژه برای پروژه‌ها</p>
                    </div>
                    <div class="card-footer">
                        <button class="btn btn-info btn-sm edit-btn" data-toggle="modal" data-target="#editAccountModal">ویرایش</button>
                        <button class="btn btn-danger btn-sm delete-btn">حذف</button>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <!-- Add Account Modal -->
    <div class="modal fade" id="addAccountModal" tabindex="-1" role="dialog" aria-labelledby="addAccountModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="addAccountModalLabel">اضافه کردن حساب بانکی جدید</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="بستن">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id="addAccountForm">
                        <div class="form-group">
                            <label for="accountNumber">شماره حساب</label>
                            <input type="text" class="form-control" id="accountNumber" required>
                        </div>
                        <div class="form-group">
                            <label for="userName">نام کاربری</label>
                            <input type="text" class="form-control" id="userName" required>
                        </div>
                        <div class="form-group">
                            <label for="accountName">نام حساب</label>
                            <input type="text" class="form-control" id="accountName" required>
                        </div>
                        <div class="form-group">
                            <label for="bankName">نام بانک</label>
                            <input type="text" class="form-control" id="bankName" required>
                        </div>
                        <div class="form-group">
                            <label for="description">توضیحات</label>
                            <textarea class="form-control" id="description" rows="3"></textarea>
                        </div>
                        <button type="submit" class="btn btn-custom">اضافه کردن</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Edit Account Modal -->
    <div class="modal fade" id="editAccountModal" tabindex="-1" role="dialog" aria-labelledby="editAccountModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="editAccountModalLabel">ویرایش حساب بانکی</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="بستن">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form id="editAccountForm">
                        <input type="hidden" id="editAccountId">
                        <div class="form-group">
                            <label for="editAccountNumber">شماره حساب</label>
                            <input type="text" class="form-control" id="editAccountNumber" required>
                        </div>
                        <div class="form-group">
                            <label for="editUserName">نام کاربری</label>
                            <input type="text" class="form-control" id="editUserName" required>
                        </div>
                        <div class="form-group">
                            <label for="editAccountName">نام حساب</label>
                            <input type="text" class="form-control" id="editAccountName" required>
                        </div>
                        <div class="form-group">
                            <label for="editBankName">نام بانک</label>
                            <input type="text" class="form-control" id="editBankName" required>
                        </div>
                        <div class="form-group">
                            <label for="editDescription">توضیحات</label>
                            <textarea class="form-control" id="editDescription" rows="3"></textarea>
                        </div>
                        <button type="submit" class="btn btn-custom">ذخیره تغییرات</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    @section Scripts {
            <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
            <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
            <script>
                $(document).ready(function () {
                    function loadAccounts() {
                        $.ajax({
                            url: '/api/BankAccount/GetAll',
                            type: 'GET',
                            success: function (data) {
                                var container = $('#accountsContainer');
                                container.empty();
                                data.forEach(function (account) {
                                    container.append(`
                                                    <div class="card">
                                                        <div class="card-header">
                                                            <h5 class="card-title text-center">${account.accountNumber.value}</h5>
                                                        </div>
                                                        <div class="card-body">
                                                            <p class="card-text"><strong>نام کاربر:</strong> ${account.userName.value}</p>
                                                            <p class="card-text"><strong>نام حساب:</strong> ${account.accountName.value}</p>
                                                            <p class="card-text"><strong>نام بانک:</strong> ${account.bankName.value}</p>
                                                            <p class="card-text"><strong>توضیحات:</strong> ${account.description.value}</p>
                                                            <div class="text-center">
                                                                <button class="btn btn-info btn-sm edit-btn" data-toggle="modal" data-target="#editAccountModal">ویرایش</button>
                                                                <button class="btn btn-danger btn-sm delete-btn">حذف</button>
                                                            </div>
                                                        </div>
                                                    </div>
                                                `);
                                });


                                // Edit button click event
                                $('.edit-btn').on('click', function () {
                                    var card = $(this).closest('.card');
                                    var accountNumber = card.find('.card-title').text();
                                    var userName = card.find('.card-text').eq(0).text().split(': ')[1];
                                    var accountName = card.find('.card-text').eq(1).text().split(': ')[1];
                                    var bankName = card.find('.card-text').eq(2).text().split(': ')[1];
                                    var description = card.find('.card-text').eq(3).text().split(': ')[1];

                                    $('#editAccountId').val(accountNumber);  // Save account number for updating
                                    $('#editAccountNumber').val(accountNumber);
                                    $('#editUserName').val(userName);
                                    $('#editAccountName').val(accountName);
                                    $('#editBankName').val(bankName);
                                    $('#editDescription').val(description);
                                });

                                // Delete button click event
                                $('.delete-btn').on('click', function () {
                                    var accountNumber = $(this).closest('.card').find('.card-title').text();
                                    if (confirm('آیا از حذف این حساب اطمینان دارید؟')) {
                                        $.ajax({
                                            url: `/api/BankAccount/Delete/${encodeURIComponent(accountNumber)}`,
                                            type: 'DELETE',
                                            success: function () {
                                                loadAccounts();
                                                showAlert('حساب بانکی با موفقیت حذف شد.', 'success');
                                            },
                                            error: function (error) {
                                                console.log('Error:', error);
                                                showAlert('خطایی رخ داد. لطفاً دوباره امتحان کنید.', 'danger');
                                            }
                                        });
                                    }
                                });
                            },
                            error: function (error) {
                                console.log('Error:', error);
                                showAlert('خطایی رخ داد. لطفاً دوباره امتحان کنید.', 'danger');
                            }
                        });
                    }

                    // Load accounts on page load
                    loadAccounts();

                    // Add account form submit event
                    $('#addAccountForm').on('submit', function (e) {
                        e.preventDefault();
                        var accountNumber = $('#accountNumber').val();
                        var userName = $('#userName').val();
                        var accountName = $('#accountName').val();
                        var bankName = $('#bankName').val();
                        var description = $('#description').val();

                        $.ajax({
                            url: '/api/BankAccount/Insert',
                            type: 'POST',
                            contentType: 'application/json',
                            data: JSON.stringify({
                                AccountNumber: accountNumber,
                                UserName: userName,
                                AccountName: accountName,
                                BankName: bankName,
                                Description: description
                            }),
                            success: function () {
                                $('#addAccountModal').modal('hide');
                                loadAccounts();
                                showAlert('حساب بانکی جدید با موفقیت اضافه شد.', 'success');
                            },
                            error: function (error) {
                                console.log('Error:', error);
                                showAlert('خطایی رخ داد. لطفاً دوباره امتحان کنید.', 'danger');
                            }
                        });
                    });

                    // Edit account form submit event
                    $('#editAccountForm').on('submit', function (e) {
                        e.preventDefault();
                        var accountNumber = $('#editAccountId').val();
                        var newAccountNumber = $('#editAccountNumber').val();
                        var userName = $('#editUserName').val();
                        var accountName = $('#editAccountName').val();
                        var bankName = $('#editBankName').val();
                        var description = $('#editDescription').val();

                        $.ajax({
                            url: '/api/BankAccount/Update',
                            type: 'PUT',
                            contentType: 'application/json',
                            data: JSON.stringify({
                                AccountNumber: accountNumber,  // Identifies the account to be updated
                                NewAccountNumber: newAccountNumber,  // New account number
                                UserName: userName,  // New user name
                                AccountName: accountName,  // New account name
                                BankName: bankName,  // New bank name
                                Description: description  // New description
                            }),
                            success: function () {
                                $('#editAccountModal').modal('hide');
                                loadAccounts();
                                showAlert('تغییرات با موفقیت ذخیره شد.', 'success');
                            },
                            error: function (error) {
                                console.log('Error:', error);
                                showAlert('خطایی رخ داد. لطفاً دوباره امتحان کنید.', 'danger');
                            }
                        });
                    });

                    // Function to show alert
                    function showAlert(message, type) {
                        var alertContainer = $('#alertContainer');
                        alertContainer.removeClass('alert-success alert-danger');
                        alertContainer.addClass('alert-' + type);
                        $('#alertMessage').text(message);
                        alertContainer.show();

                        // Hide the alert after 3 seconds
                        setTimeout(function () {
                            alertContainer.hide();
                        }, 3000);
                    }
                });
            </script>
    }
</body>

</html>
